{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        "leases": {
          ".write": "auth != null && auth.uid == $uid",
          "$acct": {
            "$pr": {
              ".validate": "newData.hasChildren(['leaseId', 'deviceId', 'operationKind', 'expiresAt']) && newData.child('expiresAt').isNumber() && newData.child('expiresAt').val() > now && newData.child('expiresAt').val() <= now + 300000 && (!data.exists() || data.child('expiresAt').val() <= now || (data.child('leaseId').val() == newData.child('leaseId').val() && data.child('deviceId').val() == newData.child('deviceId').val()))",
              "expiresAt": {
                ".validate": "newData.isNumber() && newData.val() > now && newData.val() <= now + 300000 && (!data.parent().exists() || !data.parent().child('expiresAt').exists() || data.parent().child('expiresAt').val() <= now || (data.parent().child('leaseId').val() == newData.parent().child('leaseId').val() && data.parent().child('deviceId').val() == newData.parent().child('deviceId').val()))"
              },
              "leaseId": {
                ".validate": "!data.parent().exists() || data.parent().child('expiresAt').val() <= now || data.parent().child('leaseId').val() == newData.val()"
              },
              "deviceId": {
                ".validate": "!data.parent().exists() || data.parent().child('expiresAt').val() <= now || data.parent().child('deviceId').val() == newData.val()"
              }
            }
          }
        },
        "receipts": {
          ".write": "auth != null && auth.uid == $uid",
          "$acct": {
            "$pr": {
              "$fp": {
                ".validate": "newData.hasChildren(['operationKind', 'materialVersion', 'deviceId', 'completedAt', 'outcome', 'publicationState']) && newData.child('completedAt').isNumber()"
              }
            }
          }
        },
        "dailyRounds": {
          ".write": "auth != null && auth.uid == $uid",
          "$acct": {
            "$pr": {
              "$day": {
                ".validate": "$day.matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/) && newData.child('dayPolicy').val() == 'America/Sao_Paulo'"
              }
            }
          }
        },
        "devices": {
          ".write": "auth != null && auth.uid == $uid"
        },
        "usageEvents": {
          ".write": "auth != null && auth.uid == $uid",
          "$device": {
            "$event": {
              ".validate": "newData.hasChildren(['at', 'kind', 'costUsd']) && newData.child('at').isNumber()"
            }
          }
        },
        "keyring": {
          ".write": "auth != null && auth.uid == $uid && auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && newData.hasChildren(['v', 'rev', 'updatedAt', 'epochSince', 'cur', 'kids', 'kcv', 'slots']) && newData.child('rev').isNumber() && ((!data.exists() && newData.child('rev').val() == 1) || newData.child('rev').val() == data.child('rev').val() + 1) && newData.child('updatedAt').val() <= now + 60000 && newData.child('updatedAt').val() + 60000 > now",
          "slots": {
            "$s": {
              ".validate": "$s.matches(/^(pw|pp)$/)",
              "blob": {
                ".validate": "newData.isString()"
              }
            }
          }
        },
        "live": {
          "control": {
            "admin": {
              ".write": "auth != null && auth.uid == $uid && auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && newData.hasChildren(['deviceId', 'generation', 'publicKey', 'setAt']) && newData.child('publicKey').isString() && ((!data.exists() && newData.child('generation').val() == 1) || newData.child('generation').val() == data.child('generation').val() + 1)"
            },
            "beat": {
              ".write": "auth != null && auth.uid == $uid && newData.hasChildren(['dev', 'generation', 'sequencia', 'beatAt', 'sig']) && newData.child('generation').val() == root.child('users').child($uid).child('live').child('control').child('admin').child('generation').val() && newData.child('dev').val() == root.child('users').child($uid).child('live').child('control').child('admin').child('deviceId').val() && newData.child('sequencia').isNumber() && newData.child('beatAt').isNumber() && newData.child('beatAt').val() + 60000 > now && newData.child('beatAt').val() < now + 60000"
            },
            "cleanup": {
              ".write": "auth != null && auth.uid == $uid && newData.hasChildren(['enabled', 'generation', 'rev', 'sig']) && newData.child('enabled').isBoolean() && newData.child('generation').val() == root.child('users').child($uid).child('live').child('control').child('admin').child('generation').val() && newData.child('rev').isNumber() && (!data.exists() || newData.child('rev').val() > data.child('rev').val())"
            },
            "cleanupLock": {
              ".write": "auth != null && auth.uid == $uid && (!newData.exists() || (auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && root.child('users').child($uid).child('live').child('control').child('cleanup').child('enabled').val() == true && newData.hasChildren(['dev', 'x']) && newData.child('x').val() <= now + 600000))"
            },
            "lastCleanup": {
              ".write": "auth != null && auth.uid == $uid && auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && newData.hasChildren(['at', 'dev', 'categorias'])"
            },
            "revokedBefore": {
              ".write": "auth != null && auth.uid == $uid && auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && newData.isNumber() && newData.val() < auth.token.auth_time && (!data.exists() || newData.val() >= data.val())"
            }
          },
          "devicePolicies": {
            ".write": "!newData.exists() && auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && root.child('users').child($uid).child('live').child('control').child('cleanup').child('enabled').val() == true && !root.child('users').child($uid).child('live').child('operations').exists()",
            "$dev": {
              ".write": "auth != null && auth.uid == $uid && newData.hasChildren(['v', 'generation', 'enc', 'sig']) && newData.child('generation').val() == root.child('users').child($uid).child('live').child('control').child('admin').child('generation').val() && newData.child('enc').isString() && newData.child('enc').val().matches(/^e1[.]g[0-9]+[.][A-Za-z0-9_-]+[.][A-Za-z0-9_-]+[.][A-Za-z0-9_-]+$/) && newData.child('enc').val().length <= 2048 && newData.child('v').isNumber()"
            }
          },
          "groups": {
            ".write": "!newData.exists() && auth.token.firebase.sign_in_provider == 'password' && auth.token.auth_time * 1000 + 300000 > now && root.child('users').child($uid).child('live').child('control').child('cleanup').child('enabled').val() == true && !root.child('users').child($uid).child('live').child('operations').exists()",
            "$grupo": {
              ".write": "auth != null && auth.uid == $uid && newData.hasChildren(['v', 'generation', 'enc', 'sig']) && newData.child('generation').val() == root.child('users').child($uid).child('live').child('control').child('admin').child('generation').val() && newData.child('enc').isString() && newData.child('enc').val().matches(/^e1[.]g[0-9]+[.][A-Za-z0-9_-]+[.][A-Za-z0-9_-]+[.][A-Za-z0-9_-]+$/) && newData.child('enc').val().length <= 2048 && newData.child('v').isNumber()"
            }
          }
        },
        "rulesProbe": {
          "v2": {
            "$dev": {
              ".write": "auth != null && auth.uid == $uid",
              ".validate": "!newData.exists() || newData.hasChildren(['at', 'v'])"
            }
          }
        }
      }
    }
  }
}
