# Evidência: C0 Correções da sincronização publicada

Plano: `docs/superpowers/plans/2026-09-15-md-c0-correcoes-sync.md`. Branch `md/c0`, cortada de `md/integracao` em `753bae4`.

## Estado

Implementado e validado localmente. Nenhum critério da C0 depende de validação externa: todos se provam no dublê do banco e na Engine real com `FAROL_HOME` temporário.

## Gate

    npm run check && npm run lint && npm test

| Medida | Antes (`md/integracao` com C1a) | `md/c0` |
|---|---|---|
| arquivos do `check` | 290 | 295 |
| `tests` | 2878 | 2908 |
| `pass` | 2854 | 2884 |
| `fail` | 0 | 0 |
| `skipped` | 24 | 24 |
| lint | sem regressão | sem regressão, higiene limpa |

Linhas úteis medidas (teto 400): `lib/engine/sync.js` 393, `lib/sync/receipts.js` 86, `ui/pure/sync.js` 235.

## Critério de aceite x teste x contraprova

| Critério (7.C0) | Teste | Contraprova (mutação e resultado) |
|---|---|---|
| PR bloqueado por recibo não volta à fila nem loga WARN no segundo ciclo, nem depois de reinício | `test/sync-recibo-visto-ciclo.test.js` | sem exclusão em `reconciliarVistos`: 1 falha; conjunto não lido no boot: 1; coordenador sem `marcarVistoPorRecibo`: 1; `unsee` sem limpar: 1 |
| `/api/settings` devolve `ignoradas` e a tela mostra | `test/http.test.js` (rota), `test/settings-ignoradas.test.js` | rota descartando o retorno: 1; um handler filtrando pela própria chave: 1; texto ignorando a segunda chave: 1 |
| postagem por `decide`, `already_reviewed` e `retryFailedPosts` deixa o recibo `published`; chip real | `test/sync-publicacao-recibo.test.js`, `test/ui-pure-sync.test.js` | sem publicar em `decide`: 1; em `already_reviewed`: 1; em `retryFailedPosts`: 1; escrita sem CAS: 4; sem recibo cria um: 1; invólucro ignorando coordenação desligada: 1; chip fixo em "pendente": 1 |
| lease vencido some da visão sem ciclo | `test/sync-stream.test.js` (teste do tick substituído de propósito) | sem agendar vencimento: 3; `fecharStream` sem desarmar: 5; `aoVencer` sem recalcular: 3 |
| CHANGES_REQUESTED de outro aparelho aparece como "aguardando o autor" | `test/selo-panorama-gh.test.js` | `kindDaRevisao` sem GitHub: 1; revisado sem estado virando aprovado: 1; chip sem GitHub: 2; snapshot sem campo: 1; projeção sem filtro de panorama: 1 |
| frase da tela e do README bate com os campos reais | `test/sync-privacidade.test.js` | tela sem "perfil": 1; README sem "SHA do commit": 1; campo novo no lease: 1 |
| CT-COMPAT (a): desligado não toca banco nem cria arquivo | casos de CT-COMPAT em `sync-recibo-visto-ciclo` e `sync-publicacao-recibo` | coberto pela mutação do invólucro |

Todas as restaurações conferidas byte a byte.

## Revisão do diff

- `saveSync`: a rota passou a devolver `r.sync`, então o callback `aoSalvar`, que já existia no código, roda pela primeira vez. É o comportamento pretendido pelo autor original (usar a config saneada que o servidor devolve).
- `publicarRecibo` é aguardado depois de a postagem sair e antes do toast. Nunca lança e só toca a rede com a coordenação ligada e conectada.
- Nenhum defeito concreto achado.

## Decisões técnicas (a spec não fixava)

- PR com `reviewedByMe` sem estado decisivo conhecido aparece como "revisado por você", sem botão de revisar (antes aparecia como aprovado).
- `atualizarPublicacaoDoRecibo` não cria recibo ausente e não repete a escrita depois de 412.
- "Pular" numa pendência não mexe no recibo.
- `reRequested` continua vindo só do histórico local.
