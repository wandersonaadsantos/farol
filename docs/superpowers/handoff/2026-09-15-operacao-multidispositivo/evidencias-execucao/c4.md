# Evidência: C4 Capacidade e admissão local (parte de código)

Plano: `docs/superpowers/plans/2026-09-16-md-c4-admissao-local.md`. Branch `md/c4`, da ponta de `md/integracao` com a C3 inteira.

## Estado

Parte de código implementada e validada localmente. **A etapa de medição NÃO está fechada** e não fecha aqui: ela exige execuções reais por ambiente e um critério estatístico sobre esses dados. O que esta entrega faz é deixar o ponto de coleta pronto (cada reserva guarda a métrica e o piso que usou) e declarar o piso inicial como margem, não como regra sustentada por dado.

**Fronteira (CT-COMPAT):** com o compartilhamento desligado, o escalonador é o de hoje (teto por conta). A admissão só existe com `sync.shared.enabled` ligado, e há teste de caracterização provando os dois lados.

## Gate

| Medida | Antes | `md/c4` |
|---|---|---|
| arquivos do `check` | 441 | 444 |
| `tests` | 3588 | 3606 |
| `pass` | 3560 | 3578 |
| `fail` | 0 | 0 |

## Critério de aceite x teste

| Critério (7.C4, CT-ADM) | Teste | Estado |
|---|---|---|
| Três atribuições de três contas com teto 1 abrem UMA sessão | `admissao-local` | comprovado |
| Clique e automático concorrentes não usam a mesma vaga | `admissao-local` | comprovado |
| Exceção de clique não atravessa o piso de memória | `admissao-local` | comprovado |
| Medição indisponível não admite | `admissao-local` (duas vias de medição) | comprovado |
| Espera por vaga não estaciona | `admissao-local` (fila intacta) | comprovado |
| Requisitos duros recusam desde o primeiro dia | `admissao-local` | comprovado |
| Resumo por estado e tipo, sem conteúdo | `admissao-local`, `sync-device-status` | comprovado |
| Desligado: escalonador igual ao de hoje | `admissao-desligada`, `parallel-reviews`, `justica-fila-orgs` | comprovado |
| **Medição do consumo real de memória e teste do peso do PR como preditor** | execuções reais por ambiente | **pendente, por natureza** |

## Contraprovas

11 por script, todas reprovando: medição indisponível virando suficiente, piso removido, maior em vez de menor, exceção atravessando tudo, exceção fora da contagem, presença e provedor deixando de barrar, resumo levando a reserva inteira, reserva depois de tirar da fila, admissão ligada com o compartilhamento desligado (7 falhas, inclusive nos testes antigos do escalonador) e liberação que não libera.

## Limites declarados

- **O piso (1024 MB) é margem declarada**, medida só no ambiente desta execução (Windows 11, Node 24). Ambiente novo revalida antes de confiar, e é isso que a etapa de medição vai dizer.
- **Só o caminho headless está sob a admissão.** Chat, ferramentas e sessões de terminal já são contáveis pelo módulo (o tipo existe e o resumo conta), mas a fiação deles é da entrega que mexer nesses caminhos; o contrato prevê e o código aceita.
- O teto do grupo (CT-GRUPO) continua configurado e inativo: quem liga é a C4b.
