# Evidência: A2 Plano e chaves explícito (núcleo)

Branch `md/a2`, da ponta de `md/integracao` (`d3e5b9d`, com a A3 já integrada). Spec 7.A2.

**Escopo desta entrega: o comportamento.** O cartão guiado, o selo e a tela de perfis saem do
Claude Design, bloqueado pelo login do dono (D9). O que existe agora é o que essa tela vai
chamar, mais o que não depende de layout.

## 1. O que mudou

| Item da spec | Onde | Estado |
|---|---|---|
| "Primeira validação" é ato explícito, no molde do "Testar leitura" do Jira | `POST /api/claude/profile-test` (fachada `claudeTestarPerfil`), que roda `claude auth status --json` no diretório do perfil e NUNCA grava | feito |
| Perfil materializado: pasta, nome e identidade detectada | resposta com `perfil.campos` (`configDir`, `email`, `login`, `tipoAuth`, `plano`) e o rótulo | feito |
| Nome comercial do plano não é exposto, e a tela diz isso | `plano` sai sempre `{ valor: null, origem: 'desconhecido', motivo: … }`, mesmo quando o CLI manda `subscriptionType` | feito |
| Estado de origem de cada informação | `informado`, `detectado`, `validado`, `inferido`, `desconhecido`, campo a campo | feito |
| Quem usa sem perfil recebe cartão guiado com confirmação; nenhuma escrita silenciosa | `POST /api/claude/profile-adopt`: sem `confirmar: true` literal devolve só a prévia; com ele grava um perfil e o torna padrão | núcleo feito; o cartão é a tela |
| Fim da queda silenciosa para o legado | `problemasDePerfil` (`lib/engine/perfil-claude.js`), no snapshot (`claudePerfis.problemas`) e no Diagnóstico (`runtimeChecks`), mais uma linha no Markdown do diagnóstico (A3) | feito |
| Perfil apagado aparece como erro, nunca como "usa o padrão" | o texto do check nomeia o perfil, diz que ele não existe e diz o que as sessões estão usando enquanto isso | feito |
| O selo atualiza depois do fim da sessão de login | Windows: `spawnLoginConsole` chama `doctor()` na saída, como o Codex já fazia. macOS e Linux: a sessão é marcada `login: true` e o `handleSessionExit` chama `doctor()` | feito |

**Uma fonte só de "perfil utilizável":** o `authFromProfile` saiu do `server.js` para o módulo
novo. A cascata real e o aviso leem a mesma função, senão o aviso diria que está tudo bem com
um perfil que a sessão ignora.

**A queda em si continua.** Um id apagado não derruba a revisão: ela segue na assinatura
legada da máquina. O que deixou de existir é o silêncio.

## 2. Testes

`test/perfil-claude.test.js` (21 casos, nenhum `claude` real roda; `io.runShell` é trocado):
problemas de perfil nos quatro casos e no caso saudável, snapshot e check do Diagnóstico,
teste de perfil com CLI respondendo (validado), sem login (código 1 com JSON ainda é
resposta), CLI mudo (inferido, com aviso), perfil inexistente (erro, sem rodar nada), chave
de API (nada roda e a chave não sai), padrão da máquina, adoção sem e com confirmação,
confirmação não literal, quem não tem o que adotar, fim de sessão de login (POSIX) e as duas
rotas pelo servidor HTTP real.

Ajustes declarados: `test/session-claude-profile.test.js` ganhou a asserção do `doctor()` no
fim do login do Windows (reforço do caso que já existia) e `test/local-auth-inventario.test.js`
foi de 67 para 69 rotas, com o motivo no comentário.

## 3. Contraprova

22 mutações, por script, em cópia de trabalho, restauradas byte a byte. Na primeira rodada
20 reprovaram e **duas passaram**, as duas por fraqueza do meu teste:

1. herdar a credencial da máquina no teste do perfil passava porque o ambiente do processo de
   teste não tinha `ANTHROPIC_API_KEY`. O teste passou a PLANTAR a chave, o token e o
   `GH_TOKEN` antes de cada caso e a exigir que nenhum chegue ao CLI.
2. a rota de adoção ignorando a confirmação passava porque nenhum teste exercitava a rota. O
   caso das rotas pelo servidor HTTP nasceu daí, com o valor `'sim'` provando que só `true`
   literal grava.

Na segunda rodada, as duas reprovaram.

Uma observação de infraestrutura: o caso das rotas escrito com `fetch` derrubava o processo no
fim (assert do libuv no Windows, com o `--test-force-exit` da suíte). Passou a usar
`node:http` com `Connection: close`, como os outros testes de rota; três execuções seguidas
limpas.

## 4. Gates

| Gate | Resultado |
|---|---|
| `npm run check` | verde, 522 arquivos `.js` |
| `npm run lint` | verde, sem regressão (a primeira rodada acusou `profundidadeExcedida` 17 para 19 no `http-server.js`, corrigido extraindo os dois leitores de corpo) |
| `npm test` | 3899 testes, 3871 aprovados, 28 pulados, 0 falhas |

## 5. O que falta para a A2 estar completa

1. A tela: cartão guiado da adoção, selo por perfil e a apresentação da origem de cada campo,
   pelo Claude Design.
2. Validação externa: um `claude auth status --json` real com login ativo, para conferir o
   nome do campo de e-mail (o Farol já aceita `email`, `emailAddress` e `account.email`, e cai
   para o e-mail do arquivo quando nenhum vem). Aqui só foi possível rodar o comando numa
   pasta vazia, que responde `loggedIn: false`.
3. Observação medida: `claude auth status` GRAVA `.claude.json` e `backups/` na pasta apontada.
   Por isso o teste é ato explícito, nunca rotina de boot. **Corrigido depois** (verificação A,
   16/09/2026): o "nunca grava" valia só para a configuração do Farol; o CLI alterava a pasta
   do perfil a cada teste. O status passou a rodar contra uma cópia efêmera, com a credencial
   lida da pasta original. Medição, correção e contraprovas em `verificacoes-a-b-c.md`.

---

## 6. A tela (16/09/2026, branch `md/a2-tela`)

Desenho de referência: quadro "Plano e chaves (A2)" do canvas do B2.

| Item | Onde |
|---|---|
| Botão "Testar perfil" em cada perfil, com o resultado embaixo | `claudeProfilesHtml` (`ui/pure/contas.js`) e `perfilTesteHtml` (`ui/pure/perfil.js`); o resultado fica só na tela, nunca na configuração |
| Origem de cada informação | chip por campo (informado, detectado, validado, inferido, desconhecido) e o motivo quando existe (por exemplo, por que o plano não aparece) |
| Aviso de perfil apontado que a cascata não usa | `perfilProblemasHtml`, no topo do bloco de perfis, dizendo que as sessões usam a assinatura legada enquanto isso |
| Adoção do legado pela rota do engine | o botão do cartão de migração chama `POST /api/claude/profile-adopt` com `confirmar: true`; a tela deixou de montar o perfil por fora |

**Jornada na instância isolada** (porta 47193, perfil de exemplo apontando para uma pasta
vazia e padrão do Farol apontando para um id inexistente): o aviso vermelho apareceu; "Testar
perfil" rodou o Claude Code de verdade contra a pasta isolada e mostrou "Pasta: informado",
"Login: sem login, validado", "Conta: não sabemos, desconhecido" e o plano como desconhecido. A
pasta do perfil ficou vazia depois do teste (a correção da verificação A valendo no caminho
real).

**Defeito achado ao conferir a pasta temporária:** havia 20 cópias efêmeras esquecidas por
execuções anteriores (medições da verificação A), algumas com a cópia de um `.claude.json` e
`backups/`. A limpeza da cópia no fim do teste funcionava (reproduzido: a contagem não sobe por
execução), mas um processo que morre no meio deixaria a cópia para sempre. Agora cada teste de
perfil varre as cópias com mais de uma hora (as recentes podem ser de outra execução) e a
remoção tenta de novo quando o arquivo está preso. O teste novo limpa o que criou mesmo quando
falha. As sobras desta sessão foram apagadas.

**Contraprova:** 14 mutações. Na primeira rodada, 13 reprovaram; a inerte era a asserção de que
a tela desenha o aviso, que casava o nome no import. As mutações da varredura deixaram duas
cópias para trás porque o teste não limpava ao falhar; corrigido com `finally`. Na segunda
rodada, as três refeitas reprovaram e a pasta temporária ficou limpa.

**Rodada que falhou (preservada):** a primeira suíte desta entrega terminou com `rc=1` e uma
falha de arquivo inteiro (`sync-desbloqueio`), sem caso reprovando. Investigado em vez de só
repetir: ver "Instabilidade da suíte, 16/09/2026 à tarde" no `EXECUCAO.md`.

| Gate | Resultado |
|---|---|
| `npm run check` | verde, 536 arquivos `.js` |
| `npm run lint` | verde (a primeira rodada acusou um tempo literal na varredura, trocado por `TEMPOS.HORA_MS`) |
| `npm test` | segunda rodada: 3958 testes, 3930 aprovados, 28 pulados, 0 falhas, `rc=0` |
